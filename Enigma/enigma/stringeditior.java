package enigma;

import java.util.Scanner;

public class stringeditior {
    public void substring(String input) {
        Scanner scan = new Scanner(input);
        while(scan.hasNext()) {
            if (scan.next().matches("[a-z]  ee [a-z]")) {

            }
        }
    }
}
